replicaCount: 1

image:
  repository: registry.kwer.fr/kubequest/prod/metrics-dashboard
  tag: 0.1.0
  pullPolicy: Always

imagePullSecrets:
  - name: registry-pull-secret

service:
  port: 80
  targetPort: 80

httpRoute:
  enabled: true
  hostname: metrics.kwer.fr
  gateway:
    name: cluster-edge-gateway
    namespace: traefik
