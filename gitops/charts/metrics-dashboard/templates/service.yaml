apiVersion: v1
kind: Service
metadata:
  name: {{ include "metrics-dashboard.fullname" . }}
  labels:
    {{- include "metrics-dashboard.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ include "metrics-dashboard.fullname" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
